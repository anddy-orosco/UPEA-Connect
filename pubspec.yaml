name: mi_app_universitaria
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # Dependencias principales
  shared_preferences: ^2.2.2
  image_picker: ^1.0.5
  path_provider: ^2.1.1

  # NUEVA: Para formato de fechas
  intl: ^0.19.0

  # UI y utilidades
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true